# Site Portal — Python standard library, plus:
#   Pillow — resizes pending-entry photos on upload and builds list thumbnails.
#            If it is not installed the app still runs and stores photos as-is.
pillow>=10,<13
